# signup

    > email+password -> verify email(OTP) -> done
    > email  -> verify (OTP) -> Account Creations

# login 
    > Email + password
    > Email + request OTP -> verify OTP -> Logged in 

# forgot-password
    > enter email
    > send OTP to email
    > enter OTP 
    > set new password
    > redirect to login

#  forgot-password (PasswordLess)
    >  not applied just use login with otp

# change password
    > user must be loggged in
    > and must prove that he is know the password
